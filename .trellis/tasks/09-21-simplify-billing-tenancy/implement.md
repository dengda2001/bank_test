# 实施计划：简化账单与租约

## 实施前门禁

- [x] 用户确认 PRD 的 Q1：独立账单入口收拢到月度工作台租客视角，旧 `/bills` 兼容跳转。
- [x] 用户确认 PRD 的 Q2：未来月份普通读取只预览，月份开始或明确分配预付款时才固化责任。
- [ ] 复核当前全量测试基线，将既有失败与本任务回归分开记录。
- [ ] 任务经用户审阅后执行 `task.py start`；规划阶段不修改生产代码。

## 1. 锁定账务生成契约

- [ ] 为“结构化租客不走 legacy lazy generator”编写失败测试。
- [ ] 为单人、多人均分／不等分、空房、重叠安排、重复与并发生成编写失败测试。
- [ ] 新增统一 `EnsureMonthlyRentFacts` 编排服务，复用 `ensureRentCharge`，并收窄 `ensureMonthlyObligations` 为 legacy 兼容路径。
- [ ] 将工作台、租客历史、匹配候选、现金和催收接入同一确保服务。
- [ ] 删除用户可见的“生成本月账单”动作，但保留必要兼容 handler／重定向。

验证：

```bash
go test ./cmd/truelayer-demo -run 'Test.*(RentCharge|MonthlyRent|Obligation|Workspace|Dunning|Cash|Matching).*' -count=1
```

## 2. 收敛入住与分担写入

- [ ] 以 `saveRentArrangementInTx` 为基础定义单一的房间入住计划命令和可识别错误。
- [ ] 修复结构化租客创建／编辑对 `tenants.monthly_rent_cents`、缴租日和旧文本房间字段的双重事实写入。
- [ ] 房间月租变化默认按现有比例缩放责任，提供均分与明确金额两种输入，后端严格守恒。
- [ ] 覆盖新增入住、加入同住、解绑、换房、退租、涨租、已出账锁定与跨用户冲突测试。

验证：

```bash
go test ./cmd/truelayer-demo -run 'Test.*(Arrangement|TenantRoom|RoomRent|Responsibilit).*' -count=1
```

## 3. 简化房间／租客界面

- [ ] 房间表单保留唯一的月租总额、缴租日和生效月份入口。
- [ ] 租客表单移除结构化场景下重复的月租、币种、缴租日和旧文本房间必填项。
- [ ] 增加“入住与分担”预览：房间总租金、全部入住人、默认均分、手工分担和守恒提示。
- [ ] 房间详情展示当前与历史入住／分担，不暴露内部 agreement／charge 标识为用户任务。
- [ ] 保证未绑定租客仍可创建和后续绑定。

验证：模板测试、表单往返测试，以及 1440px 与 360/390/430px 浏览器流程。

## 4. 收拢账单／租约页面与动作

- [ ] 从桌面侧栏和移动底栏移除独立账单入口，将月度责任读取与操作收拢到工作台租客视角。
- [ ] 将人工平账规范 POST 改为工作台路径，增加安全 `return_to`；保留 `/bills/settle` 兼容别名。
- [ ] 将交易详情、现金、催收和租客历史中的账单链接改为责任／房间／租客月份上下文。
- [ ] 为 `/bills`、`/tenancies` 实现兼容重定向或只读保留策略，确保筛选和月份参数可迁移。
- [ ] 更新路由、导航、移动布局和页面契约测试。

## 5. 迁移与异常数据保护

- [ ] 添加必要的非破坏性索引／约束或状态标记；不删除历史表和列。
- [ ] 构造同时包含 legacy obligation、rent charge、付款分配、现金与催收的升级夹具。
- [ ] 检测 legacy 与结构化同月冲突，阻断自动双生成并在工作台显示待处理。
- [ ] 验证外键目标和历史 ID 不变，升级和回滚不级联删除资金／催收记录。

## 6. 集成验收

- [ ] 新建房产→房间→租客选房→自动分担→打开月份→形成责任。
- [ ] 多人同住均分及手工 600/400 分担。
- [ ] 银行付款跨同房责任分配、现金补录、人工平账、撤销和催收。
- [ ] 涨租、换房和退租后历史月份保持不变。
- [ ] 旧租客仍可查看历史和处理收款；新结构化租客不产生 legacy 重复责任。
- [ ] 旧深链、筛选、分页、移动端和键盘流程通过。

最终质量门禁：

```bash
go test ./...
go vet ./...
git diff --check
```

如本地配置受控 MySQL 测试 DSN，运行相关 migration／ledger／tenant 集成测试，并确认测试数据清理为零残留。

## 风险文件与回滚点

- 账务生成：`obligations.go`、`landlord_rent_ledger.go`、`rent_workspace.go`。先独立提交，发现重复责任时可单独回滚调用点。
- 入住写入：`landlord_domain_operations.go`、`tenants.go`。保持事务边界和旧字段兼容，避免与 UI 同批大改。
- 页面／路由：`rent_collection_pages.go`、`workspace-nav.html`、`collection-settle-form.html`、`transaction_detail.go`。在领域测试通过后再切入口。
- 数据库：只追加 migration；不得编辑已发布 migration 或物理删除历史责任。

## 建议交付切片

当前 task 保留整体产品设计。用户批准后，可按风险拆为两个实施子任务：

1. **统一自动生成与入住分担领域**：完成步骤 1、2、5。
2. **精简导航、表单与兼容路由**：完成步骤 3、4，并在前者通过后启动。

最后由当前 task 执行步骤 6 的跨切片集成验收。
