# 真实 API 全链路验收：实施清单

## 前置安全门槛

- [x] 从当前路由和服务代码生成业务接口清单 `api-inventory.md`。
- [x] 确认现有认证入口、数据库配置和无现成 E2E runner/清理脚本。
- [ ] 配置并确认可丢弃的非生产 MySQL 目标、账户播种方式和受控查询方式。
- [ ] 配置可控 SMTP sink；未配置时跳过实际投递场景并明确标记为未验收。

## 纵向执行切片

### 切片 1：安全预检、认证和数据清单

- [x] 实现运行标识、目标 allowlist 预检和脱敏机器报告；dry-run 不连接目标。
- [ ] 实现同一 HTTP cookie 会话。
- [ ] 通过 `/login-local` 登录唯一隔离账户，断言 v2 session 和未授权拒绝。
- [ ] 生成可复核的 fixture/expected manifest，不把密码、DSN 或 token 写入报告。

### 切片 2：账本业务 HTTP 验收

- [ ] 通过 HTTP 创建/读取租客、更新档案和付款人关系。
- [ ] 通过受控原始流水导入创建唯一流水，并验收 EUR、非 EUR、跨月和待处理场景。
- [ ] 通过 HTTP 完成匹配、分类、拆分、续配、撤销、重复请求和跨用户拒绝。
- [ ] 通过 HTTP 完成现金预览、入账、幂等、作废、更正及余额竞争路径。
- [ ] 对每个场景记录响应并交叉读取 Dashboard、历史和流水汇总，断言金额守恒。

### 切片 3：Dashboard/催缴、报告和清理

- [ ] 验收 Dashboard 查询/筛选/分页、历史和催缴预览/发送/重试/隔离。
- [ ] 失败断言停止后续写入并保留 runID、请求、预期/实际和错误报告。
- [ ] 全部通过后按 allowlist 清理，随后通过接口与受控查询确认零残留。
- [ ] 更新验收记录、运行命令和环境限制，禁止把 skipped 当作 passed。

## 质量门禁

- `go test ./... -count=1`
- `go vet ./...`
- `git diff --check`
- runner dry-run 只能验证配置和计划，不得连接或写入未授权目标。
- 有真实环境时执行完整 runner，并保存机器可读 JSON 报告；无环境时保持任务未完成。

## 回滚/停止点

- 任何目标标识、账户前缀、外键关系或清理查询不匹配时立即停止，不扩大删除范围。
- 任意接口断言失败时保留隔离数据和报告，先诊断再决定是否单独运行 allowlist 清理。
- 不修改生产配置，不复用已有用户，不使用无运行前缀的测试实体。
