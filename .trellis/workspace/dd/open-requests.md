# 当前需求清单（2026-09-23）

| 需求 | 状态 | 验证 / 后续 |
|---|---|---|
| 首页“处理流水”展示同名流水的金额、解析月份、description、是否已匹配，供逐笔确认 | 上一轮已实现 | 本轮尚未重新验收 |
| 流水页状态筛选移到外面，仅“待处理、已关联、已忽略、全部”，默认全部 | 已完成 | Go 测试通过；Chrome 1440px 和 390px 页面检查通过 |
| 流水搜索框同时搜索付款人姓名或 ID、description、租客姓名 | 已修改，待最终验收 | Go 测试通过；MySQL 集成测试已写，需测试库运行 |
| 说明流水搜索框和日历框的实际筛选字段 | 已答复 | 日历框按流水到账月 (`transaction_time`) |
| 新建房间“保存并设置租客”可选择已有租客，或新建租客 | 已完成 | 已有租客与房间计划同事务保存；新租客进入预选房产、房间、月份的表单；MySQL 测试通过 |
| 已有租客在其他房间时高亮并禁止加入，提示先去原房间解绑；解绑后再选择 | 已完成 | 2026-09-24 用户明确改为先解绑，不做直接迁移；前端禁选和跳转，服务端冲突回滚，月份切换检查通过 |
| 房间详情“入住与租金”可直接新增租客 | 已完成 | 可从房间打开固定房产与房间的新租客表单，保存后返回原房间；移除最后一位租客可保留空房间租金规则 |
| 新增租客表单选择房产后，房间列表仅显示该房产，切换时清除不匹配的旧选择 | 已完成 | Chrome 中验证通过；Go 测试通过 |
| 流水“撤销匹配”确认页改为与流水匹配一致的工作台抽屉风格 | 已完成 | 原流水、全部有效分配、原因必填与返回路径保留；Go 测试、Chrome 桌面和手机布局检查通过 |

房间与租客的大项已完成实现与验证，记录于 Trellis 任务 `09-24-room-tenant-selection`。其他已完成项的验收状态以各行记录为准。
